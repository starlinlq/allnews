import {
    DefaultTheme
} from "styled-components";


//i need to modify this for dark mode
export const theme: DefaultTheme = {
    borderRadius: '5px',
    colors: {
        main: 'white',
        secondary: 'black'
    }
}

